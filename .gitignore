# project
_site
.sass-cache
.vagrant

# photo originals (HEIC/MOV/large) stay local; web versions live in assets/photos
Pictures/

# local tooling and scratch artifacts (keep only launch.json)
.claude/
!.claude/launch.json

# general
.DS_Store
Thumbs.db
ehthumbs.db

Gemfile.lock

beautiful-jekyll-theme-*.gem
